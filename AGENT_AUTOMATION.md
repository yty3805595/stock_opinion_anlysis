# 🤖 Agent 任务自动化系统

## RD-Agent Skill

**位置:** `skills/rd-agent/`

**功能:**
- 因子挖掘: `scripts/rd_agent_factor_mining_v2.py` (77+ 因子)
- 每日回测: `scripts/rd_agent_backtest_scheduler.py`
- 交易信号: `scripts/rd_agent_trading.py`
- 期权监控: `scripts/rd_options_tool.py`

**定时任务:**
- 09:00 - 因子挖掘 (cron 已配置)
- 10:00 - 每日回测 (cron 已配置)

## 概述

本文档定义了一个主动式的 Agent 任务分配框架，让多个 agent 自动协作，无需用户反复确认。

---

## Agent 角色定义

| Agent | 主要职责 | 触发条件 |
|-------|----------|----------|
| **main** | 协调、决策、用户交互 | 用户主动询问 |
| **trader** | 执行交易、风控检查 | 定时 + 条件触发 |
| **analyst** | 市场分析、信号生成 | 定时 |
| **monitor** | 持仓/期权监控、异常报警 | 定时 |

---

## 自动化任务调度

### 定时任务 (Cron)

| 时间 | Agent | 任务 | 状态 |
|------|-------|------|------|
| 09:00 | analyst | 每日市场分析 | ✅ |
| 09:30 | trader | 同步持仓数据 | ✅ |
| 10:00 | monitor | 期权持仓检查 | ✅ |
| 12:00 | analyst | 午盘信号更新 | ✅ |
| 15:00 | monitor | 持仓风控检查 | ✅ |
| 16:00 | trader | 日终调仓 | ✅ |
| 21:00 | analyst | 晚盘信号准备 | ✅ |

---

## 主动监控规则

### 期权监控 (monitor agent)

```python
# 触发条件
IF 期权涨跌幅 > 5% OR 到期日前2天:
    ALERT main (通知用户)
    自动计算是否需要平仓
```

### 风控监控 (trader agent)

```python
# 触发条件
IF 持仓跌幅 > 5%:
    自动执行止损
    ALERT main
IF 持仓涨幅 > 10%:
    触发移动止盈
    ALERT main
```

---

## 决策权限

**无需确认，可自动执行:**

| 场景 | 操作 | 限额 |
|------|------|------|
| 止损触发 | 自动卖出 | 单笔 < $5,000 |
| 止盈触发 | 自动卖出 | 单笔 < $10,000 |
| 期权到期 | 自动处理 | 视情况 |
| 数据同步 | 自动 | 每日 |

**需要确认:**

| 场景 | 操作 |
|------|------|
| 新建仓 | 需确认 |
| 卖出会导致亏损的持仓 | 需确认 |
| 大额操作 (> $10,000) | 需确认 |

---

## 执行流程

```
[定时触发]
    ↓
[检查权限]
    ↓
[执行 OR 请求确认]
    ↓
[记录日志]
    ↓
[通知用户]
```

---

## 关键脚本

| 脚本 | 用途 |
|------|------|
| `scripts/sync_portfolio.py` | 同步持仓数据 |
| `scripts/rd_options_tool.py` | 期权工具 |
| `scripts/rd_agent_daily.py` | 每日分析 |
| `scripts/longbridge_orders_check.py` | 订单检查 |

---

## 记住

1. **不要问"要不要做"** - 直接做自动化的部分
2. **有异常再问** - 需要用户决策时再确认
3. **每次动作都记录** - 写入 trading_journal.md
